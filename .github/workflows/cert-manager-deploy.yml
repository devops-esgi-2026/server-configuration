# name: Deploy cert-manager on K3s

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - "k3s/cert-manager/**"
#       - ".github/workflows/cert-manager-deploy.yml"
#   workflow_dispatch:

# jobs:
#   deploy:
#     name: Install cert-manager
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Prepare SSH key
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.SSH_KEY_PATH }}" > ~/.ssh/id_ansible
#           chmod 600 ~/.ssh/id_ansible
#           ssh-keyscan -H "${{ secrets.SERVER_IP }}" >> ~/.ssh/known_hosts

#       - name: Create inventory
#         run: |
#           cat > /tmp/inventory.ini <<EOF
#           [all]
#           k3s-master ansible_host=${{ secrets.SERVER_IP }}

#           [all:vars]
#           ansible_user=root
#           ansible_port=22
#           ansible_ssh_private_key_file=~/.ssh/id_ansible
#           ansible_python_interpreter=/usr/bin/python3
#           ansible_become=true
#           ansible_become_method=sudo
#           EOF

#       - name: Install Ansible
#         run: |
#           sudo apt-get update -qq
#           sudo apt-get install -y ansible

#       - name: Install cert-manager via kubectl
#         run: |
#           ansible -i /tmp/inventory.ini all -m shell -a "
#             kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.16.3/cert-manager.yaml &&
#             kubectl wait --for=condition=available --timeout=180s \
#               deployment/cert-manager \
#               deployment/cert-manager-cainjector \
#               deployment/cert-manager-webhook \
#               -n cert-manager
#           "

#       - name: Apply ClusterIssuer
#         run: |
#           ansible -i /tmp/inventory.ini all -m copy \
#             -a "src=k3s/cert-manager/ dest=/tmp/cert-manager-config/ mode=0600"
#           ansible -i /tmp/inventory.ini all -m shell -a "
#             kubectl apply -f /tmp/cert-manager-config/
#           "

#       - name: Cleanup SSH key
#         if: always()
#         run: rm -f ~/.ssh/id_ansible
